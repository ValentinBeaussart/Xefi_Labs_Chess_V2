<%= simple_form_for @player, html: { autocomplete: 'off' } do |f| %>
  <%= f.input :name, label: "Nom"%>
  <%= f.input :victory, label: "Victoire"%>
  <%= f.input :defeat, label: "Défaite"%>
  <%= f.input :position, label: "Position"%>

  <%= f.button :submit, "Valider", placeholder: 'Valider'%>
<% end %>